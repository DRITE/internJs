import React from 'react';
import ReactDOM from 'react-dom';
import PropTypes from 'prop-types';

export let PlusOne = ({value}) => {
    <div>
        <h1> {value} </h1>
        <button> +1 </button>
    </div>
}